{
  "name": "key-listener-client",
  "version": "0.0.1",
  "description": "First socket.io client to listen for keyboard events",
  "dependencies": {
    "express": "^4.15.2",
    "socket.io": "^2.2.0",
    "socket.io-client": "^2.2.0"
  }
}
